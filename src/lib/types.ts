export type PortfolioProject = {
  title: string;
  category: string;
  summary: string;
  stack: string[];
  systemTypes: string[];
  images: string[];
  link: string;
};

export type Service = {
  title: string;
  details: string;
};

export type Testimonial = {
  name: string;
  role: string;
  quote: string;
};
